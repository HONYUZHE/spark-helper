# 更新日志

## v1.0.4

- **修「无障碍明明开着，却提示要打开无障碍」**：不是无障碍被关了，是 App 进程被系统回收后
  无障碍短暂断开、系统随后自动重连；旧逻辑等 15 秒就报错，正好卡在空窗期，且报错文案误导人。
  - 找不到服务不再直接失败：把请求**挂起**，最多再等 90 秒，重连上来由无障碍服务领取补跑
  - 报错文案区分「设置里没开」和「开着的但连不上」
  - 新增常驻前台服务保活（极低优先级静默通知，可开关，默认开）
- 新增 `Application`：进程启动 / 无障碍解绑 / 销毁都写日志；全局未捕获异常处理器把崩溃栈同步落盘

## v1.0.3

- **修「点错行跑进个人主页」**：消息页的 `互动消息,XXX 在评论中提到了你` 这种行文字**包含**好友名字，
  而旧匹配规则是"包含即匹配"，于是点中了互动通知。现在只认三种候选：整段文字就是名字 /
  抖音会话行格式 `名字,预览` / 不含逗号的独立昵称控件，并加通知行前缀黑名单
- **修「启动抖音飘空、白等 30 秒」**：包可见性在同一版本的两次运行之间会飘
  （一次 `launchIntent=有`，一次 `null`），而隐式启动解析不到目标时不抛异常、只是"什么都没发生"。
  改为显式 `ComponentName` 启动 + 申请 `QUERY_ALL_PACKAGES` + 等待失败就换策略重试
- 去掉底部「消息」标签一个危险兜底（"退而取整棵树里位置最低的那个消息"，在个人主页上会点错东西）
- 新增 `ensureMainPage()`：抖音会恢复上次停留的页面，先用返回键退回有底部导航的主界面
- 匹配到的会话节点文字写进日志，便于事后定位

## v1.0.2

- **修「没找到抖音」误报**：`getLaunchIntentForPackage()` 在 Android 16 上装了也返回 null
  （同一进程里 `getPackageInfo()` 是好的），不能拿它当"是否安装"的判据
- 新增「导出到 Download」：完整日志写成 `Download/火花助手日志.txt`（API 29+ 走 MediaStore，免存储权限）
- 失败快照：界面还停在自己 App 里时跳过（没有分析价值）
- 修任务卡片「最近」永远显示"还没跑过"的问题

## v1.0.1

- 按真机控件结构修正选择器：
  - 会话行有两套节点（可点击整行 + 独立昵称控件，后者只有 140px 宽），不再用宽度过滤候选
  - 输入框查找排除「搜索相关表情包」（表情面板打开时那也是个输入框）
  - 消息页特征改用真机确认的「新关注我的 / 互动消息」
- 会话选择优先"会话列表区"，避开顶部「在线好友头像条」的同名项

## v1.0.0

- 首个版本：无障碍自动化 + 每日定时 + 30 分钟心跳补跑 + 开机重排 + 配置界面
