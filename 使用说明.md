# 火花助手 · 使用说明

每天在你设定的时间，自动打开抖音给指定好友发一条消息，维持聊天火花。
**不需要 root，不联网（APK 连 INTERNET 权限都没申请），不上传任何数据。**

---

## 一、安装

1. 把 `火花助手-vX.apk` 传到手机（微信/QQ/数据线都行）。
   **别在微信里直接点开**——微信会拦。先「保存到手机」，再从文件管理器点这个文件。
   （装过旧版直接覆盖安装即可，用的是同一个签名。）
2. 提示「不允许安装未知应用」时，点提示里的**设置**，把来源应用（文件管理器/浏览器）的
   「允许安装未知应用」打开，再回去点安装。
3. 文件管理器里找不到文件时：**别用「下载」分类**（那个视图查的是 MediaStore，
   外部写进去的文件可能不在里面），走「内部存储 → Download」直接用目录浏览。

## 二、首次设置（5 步，只做一次）

打开 App，**点顶部那块灰色状态区**，会弹出逐项菜单，按顺序点：

1. **① 去开启无障碍服务** → 在系统列表里找到「火花助手」→ 打开。
   （会提示"此服务可以查看屏幕内容"——它只监听抖音的两个包名，看不到别的 App。）
2. **② 关掉电池优化** → 直接允许。
3. **③ 允许精确闹钟** → 直接允许。
4. **④/⑤ 应用设置 + 电池设置** → 把「自启动」打开，省电策略设为「无限制」。
5. 确认下面的 **「常驻通知保活」保持勾选**（见第四节，很重要）。

然后点 **「＋ 添加任务」**：

| 字段 | 说明 |
|---|---|
| 备注 | 随便写，可空 |
| 对方名字 | **必须和抖音消息列表里显示的一致**（昵称或备注名） |
| 消息内容 | 可以写多条，用 `\|` 隔开，如 `在吗\|打卡\|早`，每次随机发一条 |
| 发送时间 | 建议设在你**经常在玩手机**的时间，比如 20:30 |

> ⚠️ 手机锁屏且有密码/指纹时，程序**没法自己解锁**。要么把时间设在你正用手机的时段，要么临时取消锁屏密码。

## 三、先试运行，别直接等定时

任务卡片上有 **「试运行」**——它会真的打开抖音、走进聊天、把消息打好，但**不点发送**。
跑完回到 App 看「运行日志」，出现「试运行结束：已找到输入框并写好内容」就说明没问题，
这时候再放心等定时执行。

## 四、「常驻通知保活」是干什么的（别关）

安卓会回收后台进程。进程一旦被回收，**无障碍服务会跟着断开几秒到几十秒**，
这时候去执行任务就会失败——而且旧版会误报成「无障碍服务没有开启」，
让用户去设置里白找一圈（设置里明明是开着的）。

保活就是让通知栏常驻一条**静默通知**（最低优先级，不响不弹不显示角标），
把进程优先级顶上去，尽量不被回收。**强烈建议保持开启。**

即使真的被回收了，新版也会：把这次请求**挂起**，等无障碍重连上来**自动补跑一次**，
不用你再点一遍。

## 五、到点没反应？

按顺序检查：

1. **运行日志**里有没有记录？
   - 「无障碍服务在系统设置里是开着的，但 App 连不上它」→ 到「设置 → 无障碍」把「火花助手」关掉再打开一次
   - 「无障碍服务没有开启」→ 回第二步①
   - 「手机锁屏且设置了密码」→ 改时间或取消密码
   - 「消息列表里没找到 XXX」→ 名字和抖音里显示的不一样
   - 「未能进入消息页 / 找不到发送按钮」→ 抖音改版了，把日志发给作者
2. 日志里**什么都没有** → 手机把 App 杀了。去设置里把「自启动 / 后台运行 / 后台弹出界面」都允许：
   - 小米/红米：应用管理 → 火花助手 → 自启动打开、省电策略「无限制」、最近任务里加锁
   - 华为/荣耀：应用启动管理 → 手动管理 → 三个开关全开
   - OPPO/一加：电池 → 应用耗电管理 → 允许后台运行
   - vivo/iQOO：电池 → 后台耗电管理 → 允许后台高耗电
3. 还是不行 → 点 **「导出到 Download」**（会生成 `Download/火花助手日志.txt`），
   把这个文件发给作者，里面含每一步的判定和失败瞬间的抖音界面快照。

## 六、日志怎么用

- **复制日志**：完整日志进剪贴板。
- **导出到 Download**：生成 `Download/火花助手日志.txt`（推荐，直接发文件最省事）。
- 列表里每条只显示第一行，完整内容用上面两个按钮看。

## 七、风险提示

抖音的用户协议**不允许**自动化操作。这个工具只在你自己的手机上、给你自己的好友发消息，
请保持低频（一天一条），消息内容尽量像人话。**账号风险由你自己承担。**

调试阶段请尽量用「试运行」，它不点发送；「立即发送」会真的把消息发出去。
